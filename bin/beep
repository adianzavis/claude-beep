#!/bin/zsh
count=${1:-1}
interval=${2:-0.15}
sound=${3:-/System/Library/Sounds/Glass.aiff}

for ((i=0; i<count; i++)); do
  if command -v afplay >/dev/null 2>&1; then
    afplay "$sound" >/dev/null 2>&1 &
  else
    /usr/bin/osascript -e 'beep 1' >/dev/null 2>&1
  fi
  sleep "$interval"
done
